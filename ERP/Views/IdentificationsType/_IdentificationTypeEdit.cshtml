@model ERP.Models.IdentificationsType

@using (Html.BeginForm("Update", "IdentificationsType", FormMethod.Post, new { @id = "registerFormId" }))
{

    <div class="container IdentificationsTypeDetails col-md-12 col-lg-12 commonPadding">
        <div class="row  col-md-12 col-lg-12ccommonPadding">
            @*@{
                    var categories = Model.ErrorList; // Cast the list
                }

                @if(Model.ErrorList != null && Model.ErrorList.Count > 0)
                {
                @foreach (var c in @categories) // Print the list
                {
                    @Html.Label(categories.ToString());
                }
                }*@

            &nbsp;
        </div>
        <div class="row  col-md-12 col-lg-12">
            <div class="col-md-6 col-lg-6">
                <div class="row col-md-12 col-lg-12">
                    <div class="float-left col-md-4 col-lg-4 lablestyles textright height-13"><div style="padding-top:14px;">IdentificationsType Name :</div></div>
                    <div class="float-right col-md-8 col-lg-8">
                        <input class="textareastyles" style="width:300px;height:35px;" type="text" name="IdentificationName" value="@(Model.IdentificationName)" placeholder="Enter IdentificationsType Name" />
                        @*<span class="input-validation-error" data-valmsg-for="IdentificationsTypeName" data-valmsg-replace="true"></span>*@

                    </div>

                </div>
            </div>

        </div>

        <div class="row col-md-12 col-lg-12 commonPadding">
            <div class="col-md-6 col-lg-6"><input type="button" data-url="@Url.Action("Update", "IdentificationsType")" class="formbutton float-right IdentificationsTypeAddEdit" data-identity="@(Model.Identity)" value="Update/Add" /></div>
            <input type="hidden" name="Identity" value="@(Model.Identity)">
        </div>



    </div>
}
